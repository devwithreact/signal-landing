# Signal landing page

A lightweight React + Vite waitlist landing page for a privacy-first visitor analytics product.

```bash
npm install
npm run dev
npm run build
```

Production assets are emitted to `dist/`.
